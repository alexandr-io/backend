apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  namespace: media
spec:
  rules:
    - host: media.alexandrio.cloud
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: api
                port:
                  number: 80