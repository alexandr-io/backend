apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  namespace: library
spec:
  rules:
  - host: library.preprod.alexandrio.cloud
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: api
            port:
              number: 80
