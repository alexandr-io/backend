---
components:
  schemas:
    GetProgressSpeedRequest:
      properties:
        word_number:
          description: Number of word to get the reading speed estimation for
          example: 100
          type: int
        language:
          description: Language of the reading speed estimation
          example: fr
          type: string

      required:
        - word_number
        - language
      type: object
