---
components:
  schemas:
    UpdatePassword:
      properties:
        current_password:
          description: The current password of the account
          example: 4bd1asd
          type: string
        new_password:
          description: The new password to set
          example: b21dwqs
          type: string
      required:
        - current_password
        - new_password
      title: UpdatePassword is the body parameter given to /password/update PUT route to update a password.
      type: object
