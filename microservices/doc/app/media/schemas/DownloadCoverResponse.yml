---
components:
  schemas:
    DownloadCoverResponse:
      description: Cover image (can be png, jpg or jpeg)
      content:
        image/png:
          schema:
            type: string
            format: binary
