version: '3'

services:
  postgres:
    restart: on-failure
    image: postgres:9-alpine
    volumes:
      - postgres-data:/var/lib/postgresql/data
    env_file:
      - ./.env

  backend:
    build: ./
    ports:
      - "420:8080"
    restart: on-failure
  
volumes:
  postgres-data: